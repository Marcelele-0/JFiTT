[project]
name = "compiler"
version = "0.1.0"
description = "Compiler for JFTT project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ply>=3.11",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0",
]
